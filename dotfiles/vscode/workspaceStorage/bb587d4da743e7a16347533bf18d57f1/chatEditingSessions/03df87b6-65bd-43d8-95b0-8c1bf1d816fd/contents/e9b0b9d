# Haciendo Debug de graficos


**Ctrl + Alt + F2 (o F3, F4...)** → cambia a otra TTY (si está disponible).

**Ctrl + Alt + Backspace** → mata el servidor gráfico actual (si tienes activada esa opción en Xorg, en Arch por defecto normalmente **no está activada**).


#### Iniciar entorno grafico manualmente
startx
o
gnome-session

#### Remover drivers de NVIDIA
sudo pacman -Rns nvidia nvidia-utils nvidia-settings nvidia-lts

##### Instalarlos nuevamente
`sudo pacman -S nvidia nvidia-utils lib32-nvidia-utils nvidia-prime`



## 🔹 2. Cargar los módulos al arranque

Edita `/etc/mkinitcpio.conf` y en la línea `MODULES=()` asegúrate de incluir:


## 🔹 3. Activar el parámetro para KMS

Edita GRUB:


## 🔹 4. Reinicia

Después del reboot, el driver NVIDIA debería estar cargado.  
Verifícalo con:


## 🔹 5. Probar PRIME Render Offload

Si todo salió bien, ejecuta:

### caso que glx not fount

---

## 🔹 1. Instala `mesa-demos`